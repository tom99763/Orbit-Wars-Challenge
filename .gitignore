# Scraped / derived data
simulation/
trajectories/
processed/

# Model checkpoints (too big + user-specific)
training/checkpoints/
*.pt

# Cron runtime state
.cron.log
.cron.lock
.cron_expiry
.cron_python

# Windows scrape task runtime state
.scrape_cron.log
.scrape_cron.log.*
.scrape_cron.lock
.scrape_cron_expiry

# Scheduler state
.scheduler.log
.scheduler.stdout
.scheduler.pid
.scheduler.lock
.first_run.log

# Training run dotfile logs/CSVs
.alphazero*
.impala*
.a2c*
.selfplay*
.synth_gen*

# Training + benchmark logs (run-local, not shared)
training/logs/

# BC dataset shards (regenerated from trajectories/<date>/<time>/)
training/bc_data/

# Kaggle artefacts
orbit-wars.zip
orbit-wars-kit.tar.gz*

# Vendored upstream library (has its own .git, re-clone if needed)
kaggle-environments/

# Submission bundles (contain model.pt + tar.gz)
submission/*/submission.tar.gz
submission/*/model.pt

# Submission helper module copies (regenerated by submission/*/build.py)
submission/*/featurize.py
submission/*/training/

# Tracking (submission monitoring)
tracking/

# Offline RL dataset + scheduler state
offline/
.offline_awac_run.log
.offline_test.log
.offline_scheduler.log
.offline_scheduler.pid
.offline_scheduler.stdout
.offline_scheduler.lock/
.offline_nightly.log

# Claude Code local
.claude/

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# WSL NTFS metadata
*:Zone.Identifier

# Large notebooks with full outputs (keep code-only notebooks)
notebooks/getting-started.ipynb
.imitation_then_eval.log
.imitation_then_eval.stdout
.eval_vs_starter.log
.lb1200_vs_starter*.log
.smoke_imitation.log
.lb1200_gen.log
